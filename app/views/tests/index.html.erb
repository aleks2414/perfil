<p id="notice"><%= notice %></p>

<h1>Listing Tests</h1>

<div class="content table-responsive table-full-width">
        <table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>

      <th>Pensamiento Lateral</th>

      <th>Liderazgo</th>

      <th>Flexibilidad</th>

      <th>Tolerancia a la Frustración</th>

      <th>Trabajo en Equipo</th>

      <th>Capacidad de Aprendizaje</th>

      <th>Comunicación</th>

      <th>Fecha</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tests.each do |test| %>
      <tr>
        <td><%= test.nombre %></td>
        <td><%= test.email %></td>

        <td><%= test.l1total %></td>

        <td><%= test.l2total %></td>

        <td><%= test.l3total %></td>

        <td><%= test.l4total %></td>

        <td><%= test.l5total %></td>

        <td><%= test.l6total %></td>

        <td><%= test.l7total %></td>

        <td><%= test.created_at.strftime(" %b-%d-%Y")%></td>


        <td><%= link_to 'Ver', test %></td>
        <td><%= link_to 'Editar', edit_test_path(test) %></td>
        <td><%= link_to 'Eliminar', test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>
<br><br>
<p>
Promedio del grupo
</p>




<%= column_chart({"Pensamiento Lateral" => @tests.average(:l1total),"Liderazgo" => @tests.average(:l2total),"Flexibilidad" => @tests.average(:l3total),"Tolerancia a la Frustración" => @tests.average(:l4total),"Trabajo en Equipo" => @tests.average(:l5total),"Capacidad de Aprendizaje" => @tests.average(:l6total),"Comunicación" => @tests.average(:l7total)}) %>

<%= javascript_include_tag "https://www.google.com/jsapi" %>



<%= link_to 'Nuevo Test', new_test_path, class: "btn button_blue" %>
